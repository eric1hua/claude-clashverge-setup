# 发布与验收记录

本文跟踪可分享版本的交付范围。个人机器历史诊断、备份及账号事件不作为公共仓库内容。

## 范围

| 项目 | 目标与验收标准 |
| --- | --- |
| 环境支持 | macOS、Clash Verge Rev 2.5.2、Mihomo 1.19.29；其他版本先停下核查 |
| 安装入口 | 手动交互、`--yes`、`--dry-run` 共用同一套部署流程 |
| 可移植配置 | 发现现有订阅与节点，从本机 DNS 与目标 Merge 继承，保留节点参数，无用户专属硬编码 |
| 变更保护 | 未知脚本保护、修改前核心校验、集中备份、漂移检查与恢复 |
| Agent 使用 | 可复制提示词、项目规则、安装与回滚文档 |
| 发布保护 | 私有计划、环境、备份、索引和个人诊断不入 Git |

## 本次发布验证

2026-09-22 发布候选验证：

| 检查 | 实际结果 |
| --- | --- |
| JavaScript 路由测试 | 10 项通过 |
| Python 准备/部署/回滚/安装/验收测试 | 61 项通过，使用临时目录和模拟 API |
| Shell 语法 | Bash 安装器与 Zsh 重载脚本通过 |
| 真实 macOS dry-run | 依赖按固定版本和哈希安装，当前订阅候选通过本机核心校验 |
| dry-run 不修改 App | 全部输入文件 SHA-256 前后一致 |
| 发布文件检查 | 白名单、私有内容与常见凭据扫描通过；不替代人工审查 |
| 完整新安装器 apply/reload | 在隔离模拟中通过；本次未在生产配置重复执行 |

GitHub CI 由每次推送触发，实际运行结果见 [Checks](https://github.com/eric1hua/claude-clashverge-setup/actions/workflows/ci.yml)。

每次安装都必须重新检查本机版本、当前订阅、候选核心配置及重载后的运行状态；发布时的源码测试不能代替目标机器验收。

## 仍需按目标机器验证

- Desktop、TUI/Claude Code 和网页的认证会话及正常工作请求。
- 各备用订阅实际激活后的规则；远程订阅更新后的正式重生成。
- 使用中的 TUN、系统代理、DNS、IPv6、UDP 与第三方 VPN/本地服务的实际路径。
- 不同操作系统、不同 App/core 版本、自定义复杂扩展组合。

## 维护触发点

- App/core 升级：复核官方实现、候选合并和运行结果，再更新适配范围。
- 节点改名或删除：预期拒绝 Claude 路由；审阅目标后显式指定现有节点。
- 新订阅：显式纳入安装目标；不能默认它已经继承其他订阅的扩展。
- 官方域名或认证流程变化：依据官方文档和实际连接证据更新规则。

本项目不设置定时任务、自动节点切换或仓库自更新。订阅刷新由 Clash Verge 自己管理。
