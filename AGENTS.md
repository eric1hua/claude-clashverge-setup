# 项目协作规则

本仓库维护适用于已有 Clash Verge Rev 订阅的 Claude 路由扩展、安装入口、验收与回滚工具。先读取 README 和操作手册，再检查当前实现；不要把历史报告当作本次部署结果。

## 文件职责

- `config/overrides/` 是可审阅的源配置，`scripts/` 是部署与验收代码，`tests/` 存放针对真实风险的测试。
- `docs/research/` 保留带日期、官方链接和版本范围的依据；`docs/operations/` 放操作流程；重要设计与版本变化分别记录到 `docs/decisions.md`、`docs/changelog.md`。
- `.local/` 存放私有依赖、生成候选及安装计划。`backups/YYYY-MM-DD/clash-verge-HHMMSS-ffffff/` 集中存储快照，manifest 记录恢复目标和校验和。禁止覆盖历史快照。
- 订阅 URL、节点明文参数、凭据、完整运行配置、私人主机地址、个人诊断与备份索引不得提交到 Git。公开证据须脱敏后另行审查。
- 修改时明确文件归属，避免多代理同时编辑同一文件；主代理负责整合、敏感信息检查和最终验收。

## 安装与修改边界

- 当前适配 macOS、Clash Verge Rev 2.5.2、Mihomo 1.19.29。新版本须重新核对官方生命周期、schema 和运行行为；不要删除版本检查来绕过不兼容。
- 优先复用 App 现有订阅关联与正式 Merge/Script 生命周期；`clash-verge.yaml` 是生成物，不是持久修改入口。不得修改上游安装包或依赖目录作为持久修复。
- 不创建订阅、不索要订阅秘密、不自动选择另一出口、不改 TUN/系统代理/时区/证书验证，不将 UDP/XUDP 参数普遍加到用户节点上。
- 保留无关规则；DNS 使用本机活动基线叠加目标 Merge，只覆盖受管理后缀并保留原 `respect-rules`，须向用户说明备用订阅的继承范围。首次使用当前来源组的物理节点，后续保留已保存的名称；节点缺失时保持受管理路由拒绝连接。
- 未识别的现有订阅脚本必须先审阅。显式 `--adopt-existing-script` 会替换整个目标脚本，备份不能替代对所丢失功能的说明。
- 用户已明确授权安装时，可使用 `./install.sh --yes`，无需重复索取常规步骤授权；仍遵守运行环境权限与具体范围。自定义逻辑取舍、未经授权的全局变更或不可逆操作须先说明具体冲突。

## 检查与报告

按“调查现状 → 生成计划 → 核心校验 → 备份部署 → App 正常重载 → 运行验收”执行。修改后运行：

```sh
node --test tests/claude-routing.test.cjs
PYTHONPYCACHEPREFIX="$PWD/.local/pycache" .local/venv/bin/python -m unittest discover -s tests -p 'test_*.py'
```

未创建私有环境时，先按安装入口准备依赖。报告必须区分纯源码测试、Mihomo 候选校验、实际部署、运行 API、匿名网络请求与认证会话。未完成的项目明确写“未验证”；CI 或安装器存在不代表已经成功执行。

配置不能保证不封号、证明地区资格或确认账号封禁原因。不实现凭据、指纹或位置伪装，不把未覆盖的流量说成全局保护。失败时保留脱敏结果和可恢复状态，不靠连续登录重试证明网络正常。
