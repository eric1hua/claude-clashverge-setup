# 变更记录

## 2026-09-22 — 初始可分享安装器

将单机配置工程整理为可分享仓库，目标适配 macOS、Clash Verge Rev 2.5.2 / Mihomo 1.19.29：

- 提供手动交互、已授权 Agent 和仅准备校验的统一安装入口。
- 通过现有订阅、来源组和准确节点名称配置 Claude 路由，不内置个人订阅、端口或节点秘密。
- 从目标机器有效 DNS 与各订阅 Merge 生成策略，添加 Claude 专用后缀，保留 `respect-rules`、其他合并字段和节点传输参数。
- 提供未知脚本保护、集中备份、漂移检查、回滚、运行验收和测试。
- 新增可复制 Agent 安装提示词、项目协作规范与通用操作文档。
- 个人诊断、完整配置、备份及索引保留为本机私有内容，不发布。

验证：10 项 JavaScript 与 61 项 Python 测试通过，Shell 语法与发布检查通过。真实 macOS `./install.sh --dry-run` 完成私有依赖安装和候选核心检查，App 输入文件校验和保持不变。完整 apply/reload 在隔离模拟中验收，本次没有在生产配置重复执行。GitHub Checks 工作流会在推送后运行；见 [发布验证记录](../PROGRESS.md)。
